Generate a professional git commit message following the strict Google/Angular commit convention based on the provided code changes.
The entire message MUST be written in English.

Format:
<type>(<scope>): <subject>

<body>

<footer>

Rules for Header (<type>, <scope>, <subject>):

1. Allowed types: feat, fix, docs, style, refactor, perf, test, chore, build, ci.
2. The <scope> must be a specific noun representing the affected module or component. Leave it empty if the change is global.
3. The <subject> MUST be in lowercase, use the imperative present tense (e.g., "add", "fix"), and MUST NOT end with a period. Keep it under 50 characters.

Rules for Body (<body>) & Verbosity:

1. **Adapt the <body> length based on the complexity of the changes:**
   - **Trivial/Minor changes** (e.g., typos, simple formatting, minor documentation): **OMIT the <body> entirely** and output ONLY the header.
   - **Moderate changes** (e.g., standard bug fixes, small feature updates): Provide a **short, one-sentence <body>** explaining _why_ the change was made. Do NOT use bullet points here.
   - **Complex/Large changes** (e.g., major features, breaking changes, architectural refactoring): Provide a detailed <body> explaining the context. Only use bullet points (-) if there are multiple distinct, critical technical sub-tasks to list.
2. If a <body> is generated:
   - It MUST start with a capital letter and follow standard English grammar.
   - Focus on _why_ the change was made, not just _what_ changed.
   - Each bullet point (if used) MUST start with a capital letter.

Rules for Footer (<footer>):

1. Use the footer only to reference issue tracking IDs (e.g., "Fixes: #123") or denote "BREAKING CHANGE: ". Otherwise, omit it.

Constraints:

- Output ONLY the raw commit message text.
- Absolutely NO markdown code blocks (```) or formatting wrappers in the final response.
